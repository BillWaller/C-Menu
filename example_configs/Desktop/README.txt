The script in this directory, install_desktop.sh, installs cmenu.desktop in your 
"$HOME"/Desktop directory and the decision.svg icon in /usr/share/icons. When 
selected, cmenu.desktop starts a ghostty terminal emulator, which then runs 
C-Menu Menu.

If you prefer your own icon, edit cmenu.desktop and replace decision.svg with 
your icon.
