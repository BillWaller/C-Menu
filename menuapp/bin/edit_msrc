#!/bin/bash
# This script is used to edit the MSRC file for a given project.

# Check if the user provided a project name
if [ -z "$1" ]; then
    echo "Usage: $0 <project_name>"
    exit 1
fi
file=$(echo "$1" | sed 's/\s.*$//')
nvim "$CMENU_HOME"/msrc/"$file"
tput smcup
