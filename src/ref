curskeys.c:    wnoutrefresh(box);
curskeys.c:            strnz__cpy(kstr, "KEY_REFRESH refresh key", KSTRLEN - 1);
curskeys.c:            wrefresh(win);
dwin.c:    // wrefresh(stdscr);
dwin.c:   initialized and, if so, it erases the screen, refreshes it, and ends the
dwin.c:    // wrefresh(stdscr);
dwin.c:    // wnoutrefresh(win_box[win_ptr]);
dwin.c:    // wnoutrefresh(win_box[win_ptr]);
dwin.c:   title of the box window if a title is provided. After resizing, it refreshes
dwin.c:    // wrefresh(stdscr);
dwin.c:   refreshes it to update the display. Use this function when you need to clear
dwin.c:    // wnoutrefresh(win);
dwin.c:   window, if they exist. It also refreshes the remaining windows to ensure the
dwin.c:            // wrefresh(win_win[win_ptr]);
dwin.c:            // wrefresh(win_box[win_ptr]);
dwin.c:        // wnoutrefresh(win_box[i]);
dwin.c:        // wnoutrefresh(win_win[i]);
dwin.c:        // refresh();
dwin.c:   all existing windows, touching and refreshing them to ensure they are redrawn
dwin.c:    // wnoutrefresh(win_box[i]);
dwin.c:    // wrefresh(win_box[i]);
dwin.c:    // wnoutrefresh(win_win[i]);
dwin.c:    // wrefresh(win_win[i]);
dwin.c:    // wrefresh(win);
exec.c:    move the cursor to the bottom, and refresh the screen before executing
exec.c:   top, refresh the screen, and restore the windows. */
exec.c:    @details Clear the screen, move the cursor to the top, and refresh the
exec.c:    wrefresh(stdscr);
exec.c:    wnoutrefresh(stdscr);
exec.c:    wrefresh(stdscr);
exec.c:        wrefresh(stdscr);
exec.c:    wnoutrefresh(stdscr);
exec.c:    wrefresh(stdscr);
exec.c:    wnoutrefresh(stdscr);
exec.c:    wrefresh(stdscr);
fields.c:            wrefresh(form->box);
fields.c:            // wrefresh(form->win);
fields.c:   field is displayed correctly within the form's window and refreshes the
form_engine.c:            wrefresh(form->box);
form_engine.c:        wrefresh(form->box);
form_engine.c:    wnoutrefresh(form->win);
form_engine.c:            wnoutrefresh(form->box);
form_engine.c:        wnoutrefresh(form->win);
form_engine.c:        wnoutrefresh(form->win);
form_engine.c:    wrefresh(stdscr);
init_view.c:    // wnoutrefresh(view->cmdln_win);
init_view.c:    // wnoutrefresh(view->ln_win);
init_view.c:   title of the box window if a title is provided. After resizing, it refreshes
menu_engine.c:        wrefresh(menu->win);
pick_engine.c:                    wrefresh(pick->win2);
pick_engine.c:    wrefresh(pick->win);
pick_engine.c:   the object's text. Turns off reverse video attribute and refreshes the pick
pick_engine.c:    wrefresh(pick->win);
pick_engine.c:    wrefresh(init->pick->win);
pick_engine.c:    wrefresh(stdscr);
pick_engine.c:    // wrefresh(pick->win2);
pick_engine.c:                wrefresh(pick->box);
pick_engine.c:                wrefresh(pick->win2);
pick_engine.c:        wrefresh(pick->box);
pick_engine.c:        wrefresh(pick->win2);
pick_engine.c:                        wrefresh(pick->box);
pick_engine.c:                wrefresh(win);
pick_engine.c:                wrefresh(win2);
pick_engine.c:                    wrefresh(pick->box);
popups.c:   popup windows are closed, such as refreshing the main window and updating any
view_engine.c:int pad_refresh(View *);
view_engine.c:                // wrefresh(view->ln_win);
view_engine.c:                pad_refresh(view);
view_engine.c:    pad_refresh(view);
view_engine.c:int pad_refresh(View *view) {
view_engine.c:        rc = prefresh(view->pad, view->pminrow, view->pmincol, view->sminrow,
view_engine.c:        rc = prefresh(view->pad, view->pminrow, view->pmincol, view->sminrow,
view_engine.c:        Perror("Error refreshing screen");
view_engine.c:    // wrefresh(view->cmdln_win);
view_engine.c:    //     wrefresh(view->ln_win);
view_engine.c:        // wnoutrefresh(view->ln_win);
view_engine.c:   overwritten by a popup or other temporary display. It uses wnoutrefresh to
view_engine.c:   wrefresh to update the physical screen. */
view_engine.c:    // wnoutrefresh(view->ln_win);
view_engine.c:    // wrefresh(view->ln_win);
view_engine.c:    // wnoutrefresh(view->cmdln_win);
view_engine.c:    // wrefresh(view->cmdln_win);
